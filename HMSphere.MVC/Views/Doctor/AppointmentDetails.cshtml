@{
    ViewData["Title"] = "Appointment Details";
}

<div class="container mt-5">
    <div class="card shadow-sm">
        <div class="card-body">
            <h4 class="card-title">Appointment Details</h4>

            @* <p><strong>Date:</strong> @Model.Date.ToString("MMMM dd, yyyy")</p> *@
            <p><strong>Date:</strong> 2024/10/15</p>
            @* <p><strong>Time:</strong> @Model.Time.ToString("hh:mm tt")</p> *@

            <div class="form-group">
                <label for="doctorName"><strong>Doctor's Name:</strong></label>
                @* <input type="text" class="form-control" id="doctorName" value="@Model.DoctorName" readonly /> *@
            </div>

            @* <p><strong>Department:</strong> @Model.Department</p> *@
            <p><strong>Department:</strong> Department</p>

            <div class="form-group">
                <label for="additionalNotes"><strong>Additional Notes:</strong></label>
                <p>gfghksjljfhuefxgsludfushkflskdlfhsdhgnknfdugslklguuhglkjfd</p>
                @* <p>@Model.Notes</p> *@
            </div>

            <div class="d-flex justify-content-start mt-3">
                <a href="" class="btn btn-dark me-2">Reschedule</a>
                <a href="" class="btn btn-outline-dark">Cancel Appointment</a>
            </div>
        </div>
    </div>
</div>

